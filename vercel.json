{
  "buildCommand": "cd ../.. && yarn install --immutable && yarn prisma generate && yarn build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && yarn install --immutable",
  "rootDirectory": "apps/web",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "env": {
    "NEXT_PUBLIC_APP_NAME": "cal.null_chapter"
  }
}
